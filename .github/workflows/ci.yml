name: CI

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
  workflow_dispatch:
  schedule:
    - cron: '0 10 * * *' # Once per day at 10am UTC

jobs:
  c-format:
    name: C code format
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Install clang-format
        # Default apt `clang-format` is 14 (22.04) or 18 (24.04); this project
        # requires >= 19 (see .clang-format / AlwaysBreakAfterReturnType).
        run: sudo apt-get update && sudo apt-get install -y clang-format-20
      - name: Check C formatting
        run: CLANG_FORMAT=clang-format-20 make c-format-check

  c-coverage:
    name: C source coverage
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Install coverage tools
        run: sudo apt-get update && sudo apt-get install -y gcovr
      - name: Run C coverage check
        run: make c-coverage-check

  test:
    name: OTP ${{ matrix.otp }} on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}

    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        otp: ['28', '29']

    env:
      ERL_LIBS: './'

    steps:
      - uses: actions/checkout@v6
      - name: Checkout Proper
        uses: actions/checkout@v6
        with:
          repository: proper-testing/proper
          path: "./proper"
      - uses: erlef/setup-beam@v1.24.0
        with:
          otp-version: ${{ matrix.otp }}
          version-type: loose
      - name: Run test
        run: |
          make
          make tests
